[Unit]
Description=Daily Docker/Nextcloud Backup Timer (Example - dev-data profile)
Documentation=file:///opt/backup-system/docs/DOCKER_NEXTCLOUD.md
Requires=backup-system@dev-data.service

[Timer]
Unit=backup-system@dev-data.service
# Run daily at 00:00 (midnight)
OnCalendar=daily
# Don't run missed backups after boot (set to true if you want catch-up)
Persistent=false
RandomizedDelaySec=10min

[Install]
WantedBy=timers.target
