# Segment 01: Validates all required configuration variables and dependencies

**Version:** 1.0.0 | **Author:** Jo Zapf | **Changed:** 2026-01-12

## Overview

Validates all required configuration variables and dependencies

## Prerequisites

**Required Environment Variables:**

- `BACKUP_DEV`
- `BACKUP_MNT`
- `BACKUP_PROFILE`
- `BACKUP_SOURCES`
- `BACKUP_UUID`
- `BORG_PASSPHRASE`
- `LOCAL_LOG_DIR`
- `REPO`
- `TARGET_DIR`

*See segment source code and [PROFILES.md](../../docs/PROFILES.md) for variable descriptions and usage.*

**Required Commands:**

- `lsof`

**Conditional Dependencies:**

- `udisksctl` - Required under certain conditions

## Behavior

**Success Path:**

```
[01] Validating configuration..."
[01] Configuration valid"
[01] Profile: ${BACKUP_PROFILE}"
[01] Sources: ${BACKUP_SOURCES}"
[01] Target: ${TARGET_DIR}"
```

**Failure Conditions:**

- BORG_PASSPHRASE is not set or empty"
- Check that secrets.env is loaded and contains BORG_PASSPHRASE"
- HDD spindown enabled but neither hdparm nor udisksctl available"
- Missing required commands: [VALUE]"
- Missing required variables: [VALUE]"

## Exit Codes

| Code | Meaning |
|------|---------|
| 0 | Success - segment completed successfully |
| 1 | Failure - see error output for details |

## Integration

**Depends on:**

- `common.env`
- `profile.env`

## Source

[`01_validate_config.sh`](../../segments/01_validate_config.sh)

---

*Generated by generate-segment-docs.sh on 2026-02-06 14:23:40*
