# Segment 05: Mounts backup device with robust error recovery

**Version:** 2.1.0 | **Author:** Jo Zapf | **Changed:** 2026-02-04 - Added multi-stage error recovery (backward compatible)

## Overview

Mounts backup device with robust error recovery

## Behavior

**Success Path:**

```
[05] Mounting backup device..."
[05] Safe unmount: ${mount_point}"
[05] Protected processes: ${NEVER_KILL_PROCESSES[*]}"
[05] Not mounted - nothing to unmount"
[05] Unmount attempt $retry/$MAX_UNMOUNT_RETRIES (normal)..."
```

**Failure Conditions:**

- All unmount strategies failed"
- Device not ready after [VALUE]s timeout"
- Device: $BACKUP_DEV"
- Expected UUID: $BACKUP_UUID"
- Found UUID: $mounted_uuid"
- Install: sudo apt install psmisc"
- Mount command failed"
- Mount reports 'already mounted' but device not found in mount table"
- Mounted device: $mounted_dev"
- Mounted: $verify_mount (UUID: $verify_uuid)"
- No processes found, but unmount still fails"
- No processes to terminate, but unmount still fails"
- Or use lazy unmount: umount -l $mount_point"
- Output: $mount_output"
- Possible kernel or filesystem issue"
- Recovery failed: Cannot unmount wrong device"
- See error messages above for details"
- Target directory not accessible: $TARGET_DIR"
- Try: sudo systemctl daemon-reload && mount -a"
- UUID validation failed after mount"
- Unknown blocking condition - try lazy unmount manually"
- Validation failed: No ext4 filesystem found at [VALUE]"
- Wrong device still mounted after mount attempt"
- fuser command not available - cannot identify blocking processes"

## Exit Codes

| Code | Meaning |
|------|---------|
| 0 | Success - segment completed successfully |
| 1 | Failure - see error output for details |

## Integration

**Depends on:**

- `BACKUP_MNT`
- `TARGET_DIR`
- `BACKUP_UUID`
- `BACKUP_DEV`

## Source

[`05_mount_backup.sh`](../../segments/05_mount_backup.sh)

---

*Generated by generate-segment-docs.sh on 2026-02-06 14:10:45*
